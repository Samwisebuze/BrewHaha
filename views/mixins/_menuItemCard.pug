mixin menuItemCard(menuitem = {})
    .menuitem
        .row
            .col-sm-8
                h2.title
                    a(href= `/drink/${menuitem.drink_id}`)
            .col-sm-4
                .price
        +rating(menuitem.rating)
        